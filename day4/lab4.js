// use a for loop to print a half pyramid of *

const num = ['*','*','*','*','*','*','*','*','*','*',]
let sum = ''

for (let i = 0; i < num.length; i++){
    sum = sum + num[i]
    console.log(sum)
}
